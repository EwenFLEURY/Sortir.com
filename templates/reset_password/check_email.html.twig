{% extends 'base.html.twig' %}

{% block title %}Réinitialisation du mot de passe{% endblock %}

{% block body %}
    <section class="container d-flex flex-column justify-content-center align-items-center" style="min-height: 70vh;">
        <div class="card shadow-lg border-0 rounded-4 p-4" style="max-width: 600px; width: 100%;">
            <div class="card-body text-center">
                <div class="mb-3">
                    <i class="bi bi-envelope-check-fill text-success" style="font-size: 3rem;"></i>
                </div>
                <h2 class="mb-3 fw-bold">Email de réinitialisation envoyé</h2>
                <p class="text-muted">
                    Si un compte associé à votre adresse email existe, un message contenant un lien de réinitialisation vient de vous être envoyé.
                </p>

                <div class="alert alert-info mt-3" role="alert">
                    Ce lien expirera dans
                    <strong>{{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}</strong>.
                </div>

                <p class="mt-3">
                    Vous n’avez rien reçu ? Vérifiez vos <strong>courriers indésirables</strong> ou
                    <a href="{{ path('app_forgot_password_request') }}" class="link-primary fw-semibold text-decoration-none">
                        renvoyez le lien
                    </a>.
                </p>

                <div class="mt-4">
                    <a href="{{ path('app_login') }}" class="btn btn-outline-primary rounded-pill px-4">
                        <i class="bi bi-arrow-left-circle me-2"></i>Retour à la connexion
                    </a>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
